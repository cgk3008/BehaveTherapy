@model IEnumerable<BehaveTherapy.Models.Plan>

@{
    ViewBag.Title = "Plan Index";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New Plan", "Create")
    </p>

    <a href="~/Account/Register">Register New User</a>
    <table class="table">
        <tr>
            <th>
                Plan Title
            </th>
            <th>
                Created
            </th>
            <th>
                Therapist
            </th>
            <th>
                Plan Is Deleted
            </th>
            <th>
                Assigned to Client
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
    {


        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Created, "{0:dd-MMM-yyyy}")
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TherapistId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsDeleted)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AssignedToUserId)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>

}

        @*@foreach (var item in Model)
    {


        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Plan.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plan.Created, "{0:dd-MMM-yyyy}")
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Therapist.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plan.IsDeleted)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AssignedToUser.FullName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Plan.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Plan.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Plan.Id })
            </td>
        </tr>

}*@


    </table>
</body>
</html>
